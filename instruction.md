Using the access log located at `/app/access.log`, analyze the traffic and aggregate the following criteria:

1. How many total requests there were
2. The number of unique IPs
3. The individual page path with the highest traffic

Save the results in JSON format to `/app/report.json`.
